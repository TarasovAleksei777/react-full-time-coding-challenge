import React, { FC } from "react";

export const Home: FC = () => {
  return (
    <div className={"h-full flex justify-center items-center"}>
      Select a task from the side menu to test it in the UI
    </div>
  );
};
